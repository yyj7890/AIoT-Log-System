# 后续开发计划

更新时间：2026-07-23

## 当前执行顺序

1. 决定群晖是否从当前已验证的`v1.1.8`升回`v1.1.9`。
2. 确定最终运行版本后，再决定是否进入 AI 日志分析阶段。

## P0 稳定性与状态流转

状态（2026-07-22）：小智普通 Wi-Fi、手机热点、断网恢复、MQTT 日志、本地 AI WebSocket 对话和远程 HiveMQ TLS 均已完成实机验证；小智固件相关修复已经完成，不再列入后续任务。群晖已从 `v1.1.6-remote-mqtt` 原地升级到 `v1.1.7-remote-mqtt`，继续复用 MySQL 数据卷和私有配置。后端停止时 API 不可达而前端仍可访问，启动后运行时长、MQTT 和原 43 条日志恢复；跨窗口恢复关闭 ID 43，再次失败生成 ID 45，状态流转为 `PENDING`/`RESOLVED`/`PENDING`，本次 MQTT 计数为 `2/2/0`。P0 升级和状态流转复测已完成；多设备与大数据量测试按当前安排暂存跳过。

- 可选留档：在 Container Manager 补充停止、启动和恢复为 `healthy` 的状态截图；功能结果已经由 API、MQTT 和数据库日志验证

完成标准：后端离线时页面不误报已接入，恢复后状态和轮询自行恢复；跨 30 秒窗口的恢复事件会关闭对应故障；状态流转、计数和既有数据均正确。

## P1 安全（暂存跳过）

状态（2026-07-20）：全局 MQTT 凭证管理和关闭匿名访问已完成实机验收。管理网页登录、HTTP 设备认证、角色权限、每设备 MQTT 凭证和公网安全能力按用户当前安排暂停，不进入当前执行路线。暂停期间系统只能用于可信网络，不得开放公网；在线演示同步暂停。

- Spring Security + JWT 登录
- 管理员、运维、只读角色
- HTTP 设备身份认证
- 每设备独立 MQTT 用户名、密码和最小权限 ACL
- 公网部署启用 TLS，禁止匿名 MQTT

学习目标：掌握 Spring Security、JWT、密码哈希、设备凭证、MQTT ACL 和 TLS。

完成标准：未登录用户不能访问管理接口；不同角色权限生效；无效设备凭证不能上报；匿名 MQTT 被拒绝。

## P2 发布与镜像质量

状态（2026-07-24）：第三阶段GHCR发布链、腾讯云TCR同步、群晖`v1.1.9`升级和`v1.1.8`回退均已完成。回退后前端、后端和远程MQTT恢复，47条数据保留；MQTT测试`1/1/0`，新增ID48且总数48。Swagger/Trace ID按预期回到旧版不可用状态。当前运行1.1.8，等待决定是否升回1.1.9。每周/手工复扫需工作流进入默认分支后生效；追加GitHub展示和在线演示仍暂存跳过。

- 已实现：为GHCR前端、后端候选镜像增加自动漏洞扫描；存在已有修复版本的CRITICAL漏洞时阻断发布
- 已完成：使用GitHub Artifact Attestation生成Sigstore签名来源证明，按镜像digest验证来源
- 已实现：复核固定版本标签不可被覆盖，远程版本标签不更新`latest`，`latest`继续保持局域网语义
- 已完成：群晖从`v1.1.9`直接回退`v1.1.8`，未删除或重建数据卷，数据和继续写入均正常
- 保持 GHCR 前后端包为 Public，并检查匿名拉取仍然可用
- 已完成：腾讯云TCR作为国内同步源；GHCR仍为主源，`v1.1.9`前后端固定标签和顶层digest已验证一致

完成标准：镜像扫描结果可见；标签规则不被破坏；群晖可以在保留数据与私有配置的前提下升级和回退；国内仓库是否采用有明确结论。

## P3 工程质量

状态（2026-07-23）：Actuator、Flyway、OpenAPI/Swagger、统一错误与日志规范均已完成。第二阶段回归基础已覆盖MQTT消息处理与QoS 1重复、故障状态流转、MySQL 8.4空库迁移/旧库基线、前端轮询暂停与自愈，以及4套Docker Compose的标签、远程边界和数据卷保护。后端共32项测试，其中本地无MySQL时30项通过、2项条件跳过；前端Vitest 4项通过。GitHub回归工作流会使用MySQL 8.4执行全部测试、前端构建和部署检查。前端性能专项、Redis与额外生产Nginx配置继续暂存跳过。

- 持续要求：核心链路变化时必须同步更新对应回归测试，不再作为一次性开发阶段

说明：OpenAPI 用于生成接口说明和调试页面，与 AI 接入无关；Flyway 后续只在结构变化时新增不可变迁移，不能修改已发布 V1；自动化测试已有基础，不要求一次补齐。

完成标准：数据库升级不依赖删除数据卷；主要 API 有可访问文档和统一错误格式；核心改动有相应回归测试。

## P4 AI 功能

- 日志智能总结
- 异常原因分析
- 维护建议
- 设备历史关联分析

学习目标：理解 Java 业务服务与 Python/AI 服务的职责边界、结构化提示和结果存储。

完成标准：AI 分析有明确输入、输出和失败处理；结果可追溯，不直接替代原始日志和规则告警。

## 可选发布方式

桌面安装包暂存跳过。当前继续使用 Docker 和网页方式部署；在线演示等待 P1 安全功能恢复后再评估。

## 其他暂存事项

- 多设备并发和大数据量性能测试
- 前端包体积与大数据页面性能专项
- Redis 与额外生产 Nginx 配置
- 追加 GitHub 展示和在线演示
- 局域网 UDP `19830` 回归
- 本地 AI 回退速度和回答能力优化；该项属于小智固件或本地 AI 服务项目
