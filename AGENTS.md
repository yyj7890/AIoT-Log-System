# AIoT Project Agent Entry

Before changing this project, read `PROJECT_CONTEXT.md`.

Working rules:

1. Treat `PROJECT_CONTEXT.md` as the concise handoff for new conversations.
2. Read `docs/project-status.md` for current completion details.
3. Read only the task-specific document linked from `docs/README.md`; do not load the full archive by default.
4. Preserve the local-development and Docker workflows. They use overlapping ports and must not run together.
5. After meaningful changes, update `PROJECT_CONTEXT.md`, `docs/project-status.md`, and the relevant focused document.
6. Consolidated source documents are preserved in `history/docs-before-consolidation-2026-07-09.zip`; do not read them unless historical detail is required.
